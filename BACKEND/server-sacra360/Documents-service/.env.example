# PostgreSQL Database Configuration
# Database Configuration (debe coincidir con docker-compose.yml)
POSTGRES_URL=postgresql://postgres:lolsito101@postgres:5432/sacra360

# Para desarrollo local (si no usas Docker)
# POSTGRES_URL=postgresql://postgres:lolsito101@localhost:5432/sacra360

# Redis Configuration
REDIS_URL=redis://redis:6379

# Service Configuration
HOST=0.0.0.0
PORT=8002

# External Services URLs (para comunicación entre microservicios)
AUTH_SERVICE_URL=http://auth-service:8001
FILES_SERVICE_URL=http://files-service:8007

# Development Settings
DEBUG=True
LOG_LEVEL=INFO

# Security (cambiar en producción)
SECRET_KEY=your-super-secret-key-change-in-production
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sacra360_documents
DB_USER=usuario
DB_PASSWORD=password

# FastAPI Configuration
DEBUG=True
SECRET_KEY=tu_clave_secreta_muy_segura_aqui

# JWT Configuration (si se usa autenticación)
JWT_SECRET_KEY=tu_jwt_secret_key_aqui
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Logging Configuration
LOG_LEVEL=INFO